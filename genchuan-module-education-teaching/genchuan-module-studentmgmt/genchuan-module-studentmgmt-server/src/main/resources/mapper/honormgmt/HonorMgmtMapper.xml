<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.studentmgmt.dal.mysql.honormgmt.HonorMgmtMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <update id="audit">
        UPDATE honor_mgmt
        SET status = #{status},
            audit_user = #{auditUser},
            audit_time = now(),
            audit_remark = #{auditRemark}
        WHERE id IN
        <foreach item="id" collection="ids" separator="," close=")" open="(" index="">
            #{id}
        </foreach>
    </update>

    <select id="selectTotalHonorCount" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM honor_mgmt
        WHERE deleted = 0
        <if test="grade != null and grade != ''">
            AND grade = #{grade}
        </if>
        <if test="major != null and major != ''">
            AND major = #{major}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="honorType != null and honorType != ''">
            AND honor_type = #{honorType}
        </if>
    </select>
    <select id="selectTodayPushCount" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM honor_mgmt
        WHERE deleted = 0 AND push_time >= DATE(NOW()) AND push_time &lt; DATE(NOW())
        <if test="grade != null and grade != ''">
            AND grade = #{grade}
        </if>
        <if test="major != null and major != ''">
            AND major = #{major}
        </if>
    </select>
</mapper>