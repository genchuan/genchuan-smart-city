<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.studentmgmt.dal.mysql.mentalmgmt.MentalMgmtMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectTotalCount" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM mental_mgmt
        WHERE deleted = 0
        <if test="mentalStatus != null and mentalStatus != ''">
            AND mental_status = #{mentalStatus}
        </if>
        <if test="riskLevel != null and riskLevel != ''">
            AND risk_level = #{riskLevel}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>

    </select>
    <select id="selectRecent7DayCount" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM mental_mgmt
        WHERE deleted = 0
        AND create_time >= DATE_SUB(CURDATE(), INTERVAL 7 DAY)
    </select>
    <select id="selectMentalStatusDistributionCount" resultType="com.alibaba.fastjson.JSONObject">
        SELECT mental_status AS name, COUNT(1) AS value FROM mental_mgmt
        WHERE deleted = 0
        GROUP BY mental_status
    </select>
    <select id="selectRiskLevelDistributionCount" resultType="com.alibaba.fastjson.JSONObject">
        SELECT risk_level AS name, COUNT(1) AS value FROM mental_mgmt
        WHERE deleted = 0
        GROUP BY risk_level
    </select>
</mapper>