<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.studentmgmt.dal.mysql.dormassign.DormAssignMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectTotalCount"
            resultType="cn.iocoder.yudao.module.studentmgmt.controller.admin.dormassign.vo.DormAssignChartRespVO">
        SELECT
            COUNT(1) AS totalCount,
            COUNT(assign_time) AS finishedCount,
            COUNT(1) - COUNT(assign_time) AS waitAssignCount,
            ROUND(COUNT(assign_time) / COUNT(1), 2) AS progress
        FROM dorm_assign
        WHERE deleted = 0
        <if test="year != null">
            AND YEAR(create_time) = #{year}
        </if>
    </select>
    <select id="selectBuildingAssignCountList" resultType="com.alibaba.fastjson.JSONObject">
        SELECT
        dorm_num AS building,
            COUNT(assign_time) AS assignCount,
            COUNT(bed_id) AS bedCount
        FROM dorm_assign
        WHERE deleted = 0
        <if test="year != null">
            AND YEAR(create_time) = #{year}
        </if>
        GROUP BY dorm_num
    </select>
    <select id="selectIndexCount"
            resultType="cn.iocoder.yudao.module.studentmgmt.controller.admin.dormassign.vo.DormAssignIndexRespVO">
        SELECT
            COUNT(1) AS totalStudentCount,
            COUNT(assign_time) AS assignedCount,
            ROUND(COUNT(assign_time) / COUNT(1), 2) AS assignRate,
            COUNT(1) - COUNT(assign_time) AS emptyBedCount
        FROM dorm_assign
        WHERE deleted = 0
        <if test="year != null">
            AND YEAR(create_time) = #{year}
        </if>
    </select>
</mapper>