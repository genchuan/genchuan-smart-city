<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.evaluate.dal.mysql.subjectmember.SubjectMemberMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <!-- 批量查询：根据主体ID列表，查所有成员 -->
    <select id="selectMembersBySubjectIds" resultType="cn.iocoder.yudao.module.evaluate.controller.admin.evalsystem.subject.vo.SubjectRespVO$SubjectMemberListRespVO">
        SELECT
        sm.subject_id AS subjectId,
        su.user_id AS userId,
        su.user_name AS userName,
        su.user_phone AS userPhone,
        sm.create_time AS joinTime
        FROM
        eval_subject_member sm
        LEFT JOIN sys_user su ON sm.user_id = su.user_id AND su.deleted = 0
        WHERE
        sm.subject_id IN
        <foreach collection="subjectIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND sm.deleted = 0
        AND sm.status_id = 1
    </select>
</mapper>