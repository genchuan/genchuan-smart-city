<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.datacenter.dal.mysql.alarm.warningalertlisttable.WarningAlertListTableMapper">

    <!-- 预警等级分布统计（统一VO） -->
    <select id="selectWarningLevelStatistics" resultType="cn.iocoder.yudao.module.datacenter.controller.admin.alarm.warningalertlisttable.vo.WarningAlertListTableStatisticsRespVO">
        SELECT
            'LEVEL' AS categoryType,
            warning_level AS category,
            COUNT(*) AS count,
    ROUND(COUNT(*) * 100.0 / NULLIF((SELECT COUNT(*) FROM gc_warning_alert_list_table WHERE deleted = b'0'), 0), 2) AS percentage
        FROM gc_warning_alert_list_table
        WHERE deleted = b'0'
        GROUP BY warning_level

        UNION ALL

        SELECT
            'STATUS' AS categoryType,
            warning_status AS category,
            COUNT(*) AS count,
    ROUND(COUNT(*) * 100.0 / NULLIF((SELECT COUNT(*) FROM gc_warning_alert_list_table WHERE deleted = b'0'), 0), 2) AS percentage
        FROM gc_warning_alert_list_table
        WHERE deleted = b'0'
        GROUP BY warning_status
        ORDER BY
            categoryType,
            CASE
            WHEN categoryType = 'LEVEL' THEN
            CASE category
            WHEN 'emergency' THEN 1
            WHEN 'important' THEN 2
            WHEN 'general' THEN 3
            WHEN 'secondary' THEN 4
            ELSE 99
        END
        ELSE 0
        END,
    CASE
        WHEN categoryType = 'STATUS' THEN count
        ELSE 0
        END DESC;
    </select>

    <!-- 预警状态分布统计（统一VO） -->
    <select id="selectWarningStatusStatistics" resultType="cn.iocoder.yudao.module.datacenter.controller.admin.alarm.warningalertlisttable.vo.WarningAlertListTableStatisticsRespVO">
        SELECT
            'STATUS' AS categoryType,
            warning_status AS category,
            COUNT(*) AS count,
            ROUND(COUNT(*) * 100.0 / NULLIF((SELECT COUNT(*) FROM gc_warning_alert_list_table WHERE deleted = b'0'), 0), 2) AS percentage
        FROM gc_warning_alert_list_table
        WHERE deleted = b'0'
        GROUP BY warning_status
        ORDER BY COUNT(*) DESC
    </select>
    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->



</mapper>