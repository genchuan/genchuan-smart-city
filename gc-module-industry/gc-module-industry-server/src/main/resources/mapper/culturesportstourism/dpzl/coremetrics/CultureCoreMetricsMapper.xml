<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.industry.dal.mysql.culturesportstourism.dpzl.coremetrics.CultureCoreMetricsMapper">

    <!-- 查询文旅资源总数 -->
    <select id="selectTotalSceneCount" resultType="cn.iocoder.yudao.module.industry.controller.admin.culturesportstourism.dpzl.coreindicators.vo.CultureCoreMetricsRespVO$CoreMetricVO">
        SELECT
            SUM(total_scene_count) AS value,
            MAX(stat_time) AS updateTime,
            0 AS warnStatus,
            #{timeCycle} AS timeCycle
        FROM stat_app_scene_rpt
    </select>

    <!-- 查询当日客流峰值 -->
    <select id="selectMaxCount" resultType="cn.iocoder.yudao.module.industry.controller.admin.culturesportstourism.dpzl.coreindicators.vo.CultureCoreMetricsRespVO$CoreMetricVO">
        SELECT
            MAX(max_count) AS value,
            MAX(stat_time) AS updateTime,
            0 AS warnStatus,
            #{timeCycle} AS timeCycle
        FROM stat_app_scene_visitor
    </select>

    <!-- 查询投诉办结率 -->
    <select id="selectCompleteRate" resultType="cn.iocoder.yudao.module.industry.controller.admin.culturesportstourism.dpzl.coreindicators.vo.CultureCoreMetricsRespVO$CoreMetricVO">
        SELECT
            AVG(complete_rate) AS value,
            MAX(stat_time) AS updateTime,
            0 AS warnStatus,
            #{timeCycle} AS timeCycle
        FROM stat_mng_matter_rpt
    </select>

    <!-- 查询设施完好率 -->
    <select id="selectFacilityGoodRate" resultType="cn.iocoder.yudao.module.industry.controller.admin.culturesportstourism.dpzl.coreindicators.vo.CultureCoreMetricsRespVO$CoreMetricVO">
        SELECT
            IFNULL(ROUND(SUM(normal_comp_count) / NULLIF(SUM(total_comp_count), 0) * 100, 2), 0) AS value,
            MAX(stat_time) AS updateTime,
            0 AS warnStatus,
            #{timeCycle} AS timeCycle
        FROM stat_app_scene_fac
    </select>

    <!-- 查询活动开展数 -->
    <select id="selectNewSceneCount" resultType="cn.iocoder.yudao.module.industry.controller.admin.culturesportstourism.dpzl.coreindicators.vo.CultureCoreMetricsRespVO$CoreMetricVO">
        SELECT
            SUM(new_scene_count) AS value,
            MAX(stat_time) AS updateTime,
            0 AS warnStatus,
            #{timeCycle} AS timeCycle
        FROM stat_app_scene_rpt
    </select>

</mapper>
