<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.chargepark.marketop.dal.mysql.couponactivity.ReceiveRecordMapper">

    <select id="selectListByTimeRange" resultType="cn.iocoder.yudao.module.chargepark.marketop.dal.dataobject.couponactivity.ReceiveRecordDO">
        SELECT * FROM receive_record
        WHERE deleted = 0
          <if test="startTime != null">
              AND receive_time &gt;= #{startTime}
          </if>
          <if test="endTime != null">
              AND receive_time &lt;= #{endTime}
          </if>
          <if test="stationId != null">
              AND station_id = #{stationId}
          </if>
    </select>

</mapper>
