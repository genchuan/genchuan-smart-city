<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.inspectop.dal.mysql.inspectuser.InspectUserMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectAreaData" resultType="cn.iocoder.yudao.module.inspectop.controller.admin.inspectuser.vo.InspectUserChartRespVO$AreaData">
        SELECT
            area AS areaName,
            COUNT(*) AS count
        FROM inspect_user
        WHERE area IS NOT NULL AND area != ''
        GROUP BY area
        ORDER BY count DESC
    </select>

    <select id="selectCardData" resultType="cn.iocoder.yudao.module.inspectop.controller.admin.inspectuser.vo.InspectUserChartRespVO$CardData">
        SELECT
            COUNT(*) AS userCount,
            SUM(CASE WHEN online_status = '1' THEN 1 ELSE 0 END) AS onlineUserCount
        FROM inspect_user
    </select>

    <!-- 查询指定人员关联的巡检任务数量 -->
    <select id="selectTaskCountByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM inspect_task
        WHERE user_id = #{userId}
          AND deleted = 0
    </select>

    <!-- 批量查询人员关联的巡检任务数量 -->
    <select id="selectTaskCountByUserIds" resultType="java.util.Map">
        SELECT
        user_id as userId,
        COUNT(*) as taskCount
        FROM inspect_task
        WHERE user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
        AND deleted = 0
        GROUP BY user_id
    </select>

</mapper>