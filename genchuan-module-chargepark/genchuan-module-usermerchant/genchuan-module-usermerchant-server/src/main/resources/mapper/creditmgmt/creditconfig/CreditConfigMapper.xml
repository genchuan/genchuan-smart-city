<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.usermerchant.dal.mysql.creditmgmt.creditconfig.CreditConfigMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <!-- 配置类型分布（饼图） -->
    <select id="selectConfigTypeDistribution" resultType="cn.iocoder.yudao.module.usermerchant.controller.admin.creditmgmt.creditconfig.vo.CreditConfigChartRespVO$ConfigTypeDistributionVO">
        SELECT remark AS type, COUNT(*) AS count
        FROM credit_config
        WHERE deleted = 0
        <if test="start != null">AND update_time >= #{start}</if>
        <if test="end != null">AND update_time &lt;= #{end}</if>
        GROUP BY remark
    </select>

    <!-- 低信用用户数（信用等级为较差或极差） -->
    <select id="selectEffectConfigCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM credit_config
        WHERE deleted = 0
        AND status = '已生效'
        <if test="start != null">AND update_time &gt;= #{start}</if>
        <if test="end != null">AND update_time &lt;= #{end}</if>
    </select>

</mapper>