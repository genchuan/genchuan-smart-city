<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.usermerchant.dal.mysql.merchantmgmt.merchantlink.MerchantLinkMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <!-- 对接类型分布（柱状图） -->
    <select id="selectLinkTypeDistribution" resultType="cn.iocoder.yudao.module.usermerchant.controller.admin.merchantmgmt.merchantlink.vo.MerchantLinkChartRespVO$LinkTypeDistributionVO">
        SELECT link_type AS type, COUNT(*) AS count
        FROM merchant_link
        WHERE deleted = 0
        <if test="start != null">
            AND create_time >= #{start}
        </if>
        <if test="end != null">
            AND create_time &lt;= #{end}
        </if>
        GROUP BY link_type
        ORDER BY count DESC
    </select>

    <!-- 对接商户数（已对接的商户去重统计） -->
    <select id="selectLinkMerchantCount" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT merchant_id)
        FROM merchant_link
        WHERE deleted = 0
        AND status = '已对接'
        <if test="start != null">
            AND create_time >= #{start}
        </if>
        <if test="end != null">
            AND create_time &lt;= #{end}
        </if>
    </select>

    <!-- 对接成功率（已对接记录数 / 总记录数） -->
    <select id="selectLinkSuccessRate" resultType="java.math.BigDecimal">
        SELECT ROUND(
        IFNULL(
        SUM(CASE WHEN status = '已对接' THEN 1 ELSE 0 END) /
        NULLIF(COUNT(*), 0)
        , 0), 2
        ) AS successRate
        FROM merchant_link
        WHERE deleted = 0
        <if test="start != null">
            AND create_time >= #{start}
        </if>
        <if test="end != null">
            AND create_time &lt;= #{end}
        </if>
    </select>

</mapper>