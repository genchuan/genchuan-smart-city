<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.stationresource.dal.mysql.stationresource.rulecontrol.timepermission.TimePermissionMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <!-- 统计总使用次数 -->
    <select id="selectTotalUseCount" resultType="java.lang.Integer">
        SELECT IFNULL(SUM(use_count), 0)
        FROM time_permission
        WHERE deleted = 0
    </select>

    <!-- 按月分组统计使用次数 -->
    <select id="selectUseCountGroupByMonth" resultType="cn.iocoder.yudao.module.stationresource.controller.admin.stationresource.rulecontrol.timepermission.vo.statistics.TimePermissionChartRespVO$UseLine">
        SELECT DATE_FORMAT(create_time, '%Y-%m') AS date,
               IFNULL(SUM(use_count), 0) AS useCount
        FROM time_permission
        WHERE deleted = 0
        GROUP BY DATE_FORMAT(create_time, '%Y-%m')
        ORDER BY date ASC
    </select>
</mapper>
