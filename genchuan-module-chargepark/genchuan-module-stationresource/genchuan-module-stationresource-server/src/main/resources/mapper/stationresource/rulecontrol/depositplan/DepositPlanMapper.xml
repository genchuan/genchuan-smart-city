<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.stationresource.dal.mysql.stationresource.rulecontrol.depositplan.DepositPlanMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <!-- 查询卡片统计：生效方案数 + 总订单量 -->
    <select id="selectCardData" resultType="cn.iocoder.yudao.module.stationresource.controller.admin.stationresource.rulecontrol.depositplan.vo.ops.DepositPlanChartRespVO$CardDataVO">
        SELECT COUNT(*) AS enablePlanCount,
               IFNULL(SUM(deposit_order_count), 0) AS totalDepositOrderCount
        FROM deposit_plan
        WHERE status = '已生效'
          AND deleted = 0
    </select>

    <!-- 查询场景柱状图：按场景统计订单总量 -->
    <select id="selectSceneBarList" resultType="cn.iocoder.yudao.module.stationresource.controller.admin.stationresource.rulecontrol.depositplan.vo.ops.DepositPlanChartRespVO$SceneBarVO">
        SELECT scene AS name, IFNULL(SUM(deposit_order_count), 0) AS value
        FROM deposit_plan
        WHERE deleted = 0
        GROUP BY scene
        ORDER BY value DESC
    </select>
</mapper>
