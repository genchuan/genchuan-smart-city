<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.integratedsecurity.dal.mysql.videomonitor.realtimemonitor.RealTimeMonitorMapper">

    <select id="selectAreaStatList" resultType="cn.iocoder.yudao.module.integratedsecurity.controller.admin.videomonitor.realtimemonitor.vo.RealTimeMonitorChartRespVO$AreaStat">
        SELECT
            area,
            COUNT(*) AS count,
            SUM(CASE WHEN run_status = '1' THEN 1 ELSE 0 END) AS onlineCount,
            SUM(CASE WHEN run_status != '1' THEN 1 ELSE 0 END) AS offlineCount
        FROM real_time_monitor
        WHERE deleted = 0
        GROUP BY area
    </select>

    <select id="selectCameraMapList" resultType="cn.iocoder.yudao.module.integratedsecurity.controller.admin.videomonitor.realtimemonitor.vo.RealTimeMonitorChartRespVO$CameraMapItem">
        SELECT id, camera_name AS cameraName, lon, lat, run_status AS runStatus
        FROM real_time_monitor
        WHERE deleted = 0
    </select>

</mapper>